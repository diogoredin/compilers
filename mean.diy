public void printr(real r)
public void srand(integer i)
public integer rand()

public integer entry (integer argc, string *argv, string *envp) {
    if argc >= 0 then {

        integer i := 0
        real mean := 0.0

        do {
            real mean := mean+rand(argv[1])
            i++;
        } while (i <= argv[1]);

        real mean := mean/i
        printr(r)
    }
    entry := 0
};